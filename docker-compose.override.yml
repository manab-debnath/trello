services:
  db:
    ports:
      - "5433:5432"

  redis:
    image: redis/redis-stack:7.4.0-v8
    container_name: redis_stack
    ports:
      - "6379:6379"
      - "8001:8001"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
    driver: local
